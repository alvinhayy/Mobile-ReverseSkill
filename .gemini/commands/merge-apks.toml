# Generated by scripts/sync-providers.sh — edit commands/<name>.md instead.
description = "Merge split APKs / XAPK / APKS / APKM into one installable, signed APK (APKEditor) (args: \"<dir|xapk|apks|apkm> [out.apk]\")"

prompt = """
Merge a split app into a single installable APK so patching / reFlutter / apk-mitm work:
```bash
scripts/merge-apks.sh {{args}}      # downloads APKEditor on first run, merges + signs
```
Then `scripts/patch-apk.sh install <out.apk>`. Needed because most tooling can't operate on
split/XAPK bundles (\"App not installed\"). Authorized targets only.
"""
