# Generated by scripts/sync-providers.sh — edit commands/<name>.md instead.
description = "Pull an app (base APK + all splits) from a connected device/AVD via adb — resolves pm path, handles work profiles, merges split apps into one signed APK (args: <package-name>)"

prompt = """
Pull the app from the attached device/AVD into `targets/`: `{{args}}`

Steps:
1. Resolve every APK path (base + splits):
   `adb shell pm path {{args}}` → lines like `package:/data/app/.../base.apk`,
   `.../split_config.arm64_v8a.apk`, …
   Not found? It may live in a work profile / secondary user — `adb shell pm list users`,
   then retry `adb shell pm path --user 10 {{args}}` and add `--user 10` to the pull.
   Unsure of the exact package name: `adb shell pm list packages -3 | grep <keyword>`.
2. Pull each path (strip the `package:` prefix): `adb pull <path> targets/{{args}}/`.
3. Multiple splits → merge into one installable signed APK:
   `scripts/merge-apks.sh targets/{{args}}` (APKEditor) → `targets/{{args}}/merged-signed.apk`.
   Single `base.apk` → use it directly.
4. Triage what was pulled: `scripts/detect-stack.sh targets/{{args}}/*.apk` (flutter | rn | native →
   picks the right analysis pipeline).
5. Suggest next steps: `/re-static` (endpoints/secrets/deception), `/vuln-hunt` (vuln classes),
   `/patch-apk` (decompile → patch → re-sign).

Authorized targets only — pull apps you own or are contracted to test. Pulled APKs stay under
`targets/` (git-ignored); never commit them.
"""
